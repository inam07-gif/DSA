arr=[5,2,4,6,1,3]

for i in range(0,len(arr)):
    small=arr[i]
    track=i
    for j in range(i+1,(len(arr))):
        if small>arr[j]:
            small=arr[j]
            track=j
    temp=arr[i]
    arr[i]=small
    
    arr[track]=temp
    
    
    
print(arr)

