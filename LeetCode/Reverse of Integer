class Solution(object):
    def reverse(self, x):
        
        
            sign= -1 if x<0 else 1
            reversed_val=sign*int(str(abs(x))[::-1])
            if reversed_val >2147483647 or reversed_val< -2147483647:
                return 0
            return reversed_val
       
       
        
